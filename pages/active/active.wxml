<!--pages/active/active.wxml-->
<map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="14" controls="{{controls}}" bindcontroltap="controltap" markers="{{markers}}"  show-location="{{true}}" style="width: 100%; height: 100vh;" bindmarkertap="addrClick"></map>
